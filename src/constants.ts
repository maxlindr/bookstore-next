export const FAVORITES_STORAGE_KEY = 'favorites';

export enum Route {
  Home = '/',
  Favorites = '/favorites',
}

export enum TextResource {
  BookDetailsTitle = 'О товаре',
  BookDescriptionTitle = 'О книге',
  AddToFavorites = 'Добавить в избранное',
};
